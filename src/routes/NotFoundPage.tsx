import React from 'react';
import Center from '~src/components/common/primitive/Center';

const NotFoundPage: React.FC = () => {
  return <Center>Page your are looking for is not available</Center>;
};

export default NotFoundPage;
